# Design System — MyCut

> MyCut 的设计唯一标准。做任何 UI/视觉决策前先读这里。
> 方向：**克制专业 / Calm Premium**。安静、精确、留白多、以中性大面为主，
> 用一条克制的桃橙到粉色渐变建立品牌识别。给"认真做内容的人"用，不是玩具风、不是冷黑科技风。

## Product Context
- **是什么**：AI 视频剪辑 Agent。查热点 → 生成大纲/文案 → 本地上传视频 → 自动切片 + 匹配字幕，串成从选题到成片的创作闭环。
- **给谁用**：内容创作者 / 剪辑者。
- **类型**：macOS 桌面应用（Tauri + React + TypeScript）。当前用 Ant Design，需按本系统逐屏重做。
- **记忆点**：有调性、克制、专业——让人觉得"这是认真的好工具"。

## Aesthetic Direction
- **方向**：Calm Premium / 克制编辑感。
- **装饰级别**：minimal（排版和留白做所有的活，不堆装饰）。
- **气质**：安静、精确、从容、高级。大量留白、低信息密度、发丝级分隔。
- **参考**：产品 Figma Demo 的克制大面与轻阴影，并采用接近 iOS 的分级圆角；字体由 MyCut 自己的 MiSans 体系统一。
- **明确不要**：玩具撞色、彩色 chip、全屏渐变、紫色 AI 渐变、霓虹、3 列图标网格、死黑 `#000`。

## Color

双主题（浅色为主，深色等价）。品牌渐变**极克制**：只用于品牌标题、主动作、进行中状态与稀疏环境光；其余使用中性色。

### 浅色（默认）
```css
--bg:        #FFFFFF;  /* 工作区统一纯白，保持洁净 */
--card:      #FFFFFF;
--ink:       #1A1A19;  /* 柔近黑正文，非纯黑 */
--sub:       #6E6B66;  /* 次级文字 */
--muted:     #A8A59F;  /* 弱化/占位 */
--line:      #EBE9E4;  /* 发丝边框 */
--line-2:    #F5F5F6;  /* 控件与信息面的中性底 */
--accent:    #E84D8D;  /* 渐变不可用时的品牌回退色 */
--brand:     linear-gradient(90deg, #FFC99E 1%, #FF87A4 67%, #FF6BA6 100%);
--thumb:     #EEECE8;  /* 缩略图占位底 */
--cta-bg:    #1A1A19;  /* 主按钮：实心墨 */
--cta-fg:    #FFFFFF;
```

### 深色
```css
--bg:        #151416;  /* 暖近黑，非纯黑 */
--card:      #211F22;
--ink:       #ECEAE6;  /* 暖白正文 */
--sub:       #A6A29B;
--muted:     #76726C;
--line:      #2C2A2D;
--line-2:    #232124;
--accent:    #FF87AC;
--brand:     linear-gradient(90deg, #F2AD7C 1%, #F2769B 67%, #EF5F9C 100%);
--thumb:     #262428;
--cta-bg:    #ECEAE6;  /* 主按钮：浅墨 */
--cta-fg:    #19181A;
```

### 语义色（两主题通用，克制使用）
```css
--ok:    #5BB36A;  /* 已完成（小圆点） */
--warn:  #E6B23C;
--error: #E66A5C;  /* 失败 */
--info:  var(--accent);
```
- **暗色策略**：不是把浅色反相，而是重设表面层级，并降低品牌光晕透明度，保证工具界面的专注感。

## Typography

全部文字统一使用 `MiSans`，包括中文、英文、品牌字标、数字与时间码。层级由字号、真实字重、行高和留白建立，不再依赖衬线或等宽字体混排。

- **字体栈**：`"MiSans", "Mi Sans", "PingFang SC", "Microsoft YaHei", sans-serif`。
- **真实字重**：Regular `330`、Medium `380`、Semibold `520`、Bold `630`；不使用浏览器伪粗体或伪斜体。
- **数字 / 时间码**：仍使用 MiSans，并开启 `font-variant-numeric: tabular-nums` 保证对齐。
- **字体来源与许可**：见 `FONT-NOTICE.md`。
- **字号阶**（rem，16px 基准）：
  | 角色 | 字号 | 字重 | 行高 |
  |------|------|------|------|
  | wordmark | 28px | 520 | 1.1 |
  | 区块标题 h2 | 16px | 520 | 1.3 |
  | 卡片标题 | 15px | 380 | 1.45 |
  | 正文 | 14–15px | 330 | 1.5 |
  | 次级/元信息 | 12.5–13px | 330/380 | 1.4 |
  | 标签/uppercase | 11px / letter-spacing .8px | 380 | — |

## Spacing
- **基准**：4px。
- **密度**：spacious（留白多、信息少）。
- **阶**：`2(2) xs(4) sm(8) md(16) lg(24) xl(32) 2xl(40) 3xl(56)`。
- **常用**：窗口内边距 56px；区块间距 56px；卡片网格 gap 24px；卡片内边距 20–22px；导入框与首项目区之间大留白。

## Layout
- **方式**：grid-disciplined（克制网格），不玩编辑式破格。
- **项目网格**：3 列（大间距、大卡片），窄屏降到 2/1 列。
- **最大内容宽度**：跟随窗口，左右 56px 安全边距。
- **圆角**：按 iOS 式层级统一：控件 `12px`、媒体 `16px`、卡片 `20px`、大型面板/弹窗 `24px`。只允许徽章、头像与明确的状态点使用全圆角。
- **边框**：主要面板优先使用留白和柔和阴影建立层级；高密度控件才使用发丝级边框。

## Components
- **主按钮**：品牌渐变，`12px` 圆角，使用白色文字与白色面性图标；每个视图只保留一个。文字 14px/380。
- **次按钮**：中性浅底或发丝边框，`12px` 圆角，文字 `--sub`。
- **分段切换**：`12px` 圆角容器使用 `--line-2` 底，选中项 `--card` 底 + 极轻阴影。
- **主导航**：未选中使用 Solar Linear；选中使用对应 Solar Bold，并以桃橙到粉色渐变填充，选中项背景仅使用中性浅灰。
- **素材卡片**：采用剪辑工具式媒体卡片：16:9 预览区、评分/时长覆盖层、标题与时间码、明确的预览和下载动作；不再用空白大容器包围单张卡片。
- **状态表达（克制）**：缩略图左上角小圆点 + 文字标签（`下载中`/`分析中`=accent、`已完成`=ok、`失败`=error）。进度用 4px 细线（accent），右侧百分比用 MiSans 等宽数字。
- **已完成元信息**：纯灰 MiSans 等宽数字 + 中点分隔，例 `7 切片 · 1 合集 · 8 个月前`，**不用彩色 chip**。
- **阴影**：极轻。`0 1px 2px rgba(0,0,0,.03), 0 8px 24px rgba(0,0,0,.04)`（深色相应降透明度）。

## Motion
- **方式**：minimal-functional + 个别 intentional。只做有意义的过渡，不炫技。
- **缓动**：进入 `ease-out`、退出 `ease-in`、位移 `ease-in-out`。
- **时长**：micro 80–120ms、short 150–250ms。
- **微交互**：卡片 hover 上移 2px + 极轻阴影；进度线平滑过渡。
- **一个值得做的高光**：「出片成功」给一个克制但有仪式感的过渡（无彩纸、无弹跳），服务"想分享"。

## Decisions Log
| 日期 | 决策 | 理由 |
|------|------|------|
| 2026-05-31 | 初版设计系统 | /design-consultation 产出。方向几经迭代（暖编辑→玩具风→否决）最终锁定 Dia 式「克制专业」，浅/深双主题，去大标题、低密度、单一克制蓝。 |
| 2026-08-24 | shadcn 基础层 + MiSans | 保留 Light/Dark；所有文字统一 MiSans。先建立语义 Token 与组件基础层，再逐屏迁移，不改变现有架构和业务流程。 |
| 2026-08-24 | Figma 品牌语言 | 从产品 Demo 提取暖白大面、32px 圆角、柔和阴影与桃橙到粉色渐变。渐变成为品牌色，但只用于主动作和关键状态。 |
| 2026-08-25 | 专业工具视觉规范 | 浅色工作区改为纯白；圆角收敛为 12/16/20/24px；导航选中态使用渐变 Solar Bold；项目页采用剪辑软件式媒体卡片。 |
