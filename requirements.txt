fastapi
uvicorn[standard]
sqlalchemy
alembic
celery[redis]
redis
pydantic
pydantic-settings
python-multipart
websockets
# Supabase 认证：后端校验前端传来的 Supabase JWT（HS256）
pyjwt>=2.8.0
# 支付宝支付：官方 Python SDK（电脑网站支付 alipay.trade.page.pay）
alipay-sdk-python>=3.7.0
requests
aiohttp
aiofiles
pytest
pytest-cov
pytest-mock
cryptography
pysrt
psutil
pytz

# 自动成片（Remotion 合成路线）：TTS 配音。免费免 key，中文声音自然。
edge-tts

# LLM provider SDKs — required for the AI clipping pipeline. These used to live
# only in install_llm_dependencies.py (installed separately), so the portable
# desktop build shipped without them and any provider call failed. Bundle them
# so the desktop app works out of the box.
openai>=1.0.0
# 拼贴空镜（collage_service）默认走 OpenRouter 静帧 + MiniMax 首尾帧，两条都是纯 requests，
# **不需要**这个 SDK。只有把 COLLAGE_VIDEO_ENGINE 切成 omni（Gemini Omni Flash）时才要 >=2.10.0
# （Interactions API 的下限）；静帧切 gemini 直连 1.x 就够。所以这里不抬版本，避免全员重装。
google-genai>=1.0.0
dashscope>=1.10.0
